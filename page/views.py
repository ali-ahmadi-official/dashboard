import datetime
import jdatetime
import json
import os

from openai import OpenAI
from collections import defaultdict

from django.http import JsonResponse
from django.db.models import Max
from django.db.models.functions import Substr
from django.views.decorators.csrf import csrf_exempt
from django.shortcuts import render, redirect, get_object_or_404
from django.conf import settings
from django.contrib.auth import authenticate, login
from django.contrib.auth.models import User

from .models import Conversation, Message

TITLE_SYSTEM_PROMPT = """
تو یک دستیار هوش مصنوعی فارسی هستی که فقط یک وظیفه داری: ساخت عنوان کوتاه برای یک گفتگوی جدید بین بیمار پیوند کلیه و سیستم پاسخ‌دهی پزشکی.

قوانین:
- فقط بر اساس همین یک پیام که بیمار فرستاده (پیام اول گفتگو)، یک عنوان کوتاه و مرتبط با موضوع پیام بساز.
- عنوان حداکثر ۴ تا ۶ کلمه باشد.
- از علائم نگارشی غیرضروری (نقطه پایانی، گیومه، پرانتز، ایموجی) استفاده نکن.
- فقط و فقط متن خام عنوان را برگردان؛ هیچ توضیح، مقدمه، پیشوند یا کد اضافه نکن.
- اگر پیام بیمار خیلی کوتاه، نامفهوم یا کلی بود، بازهم یک عنوان کوتاه و معقول بر اساس بهترین برداشت از محتوا بساز (مثلاً «سوال درباره تغذیه بعد از پیوند»)، هرگز خالی برنگردان.
"""

MAIN_SYSTEM_PROMPT = """
تو یک دستیار هوش مصنوعی فارسی برای پاسخ‌گویی به بیماران پیوند کلیه هستی. پاسخ‌ها باید واضح، روان، دقیق و مناسب لحن گفت‌وگو با بیمار باشند.

تمام پیام‌هایی که من برای تو ارسال می‌کنم شامل تاریخچه مکالمه، پیام جدید بیمار و ساعت و تاریخ شمسی کنونی مکالمه هستند.

در تاریخچه مکالمه:
- متن‌هایی که با عبارت «پیام بیمار» مشخص شده‌اند، پیام‌های قبلی بیمار هستند.
- متن‌هایی که با عبارت «پاسخ هوش مصنوعی» مشخص شده‌اند، پاسخ‌های قبلی تو هستند.

بنابراین پاسخ خود را همیشه بر اساس موارد زیر تولید کن:
1. پیام جدید بیمار
2. تاریخچه کامل مکالمه
3. ساعت و تاریخ شمسی کنونی که من در پیام ارائه می‌کنم

هرگز ساعت یا تاریخ شمسی کنونی را از بیمار نپرس و برای تعیین زمان فعلی فقط از تاریخ و ساعتی که من ارائه می‌کنم استفاده کن.

قوانین محاسبه، پارامترها، نمونه سؤال‌ها و پاسخ‌های مربوط به موضوعات تخصصی نیز در ادامه پیام به تو داده می‌شوند.

## وظیفه اصلی تو

1. ابتدا موضوع و هدف پیام جدید بیمار را با «متن سوال بیمار نمونه» در داده‌های ارائه‌شده مقایسه کن.

2. اگر پیام بیمار با یکی از سوال‌های نمونه مطابقت داشت:
   - تمام پارامترهای لازم آن سوال را شناسایی کن.
   - تاریخچه مکالمه را بررسی کن تا مشخص شود کدام پارامترها قبلاً توسط بیمار پاسخ داده شده‌اند.
   - فقط پاسخ‌های واقعی و واضح بیمار را برای تعیین مقدار پارامترها در نظر بگیر.
   - پارامترهایی که بیمار قبلاً به‌طور واضح پاسخ داده است را دوباره از او نپرس.
   - پارامترهایی که هنوز پاسخ داده نشده‌اند یا مقدارشان مبهم و ناقص است را از بیمار درخواست کن.
   - اگر چند پارامتر باقی مانده است، همه پارامترهای موردنیاز باقی‌مانده را در یک پاسخ از بیمار درخواست کن.
   - تا زمانی که تمام پارامترهای لازم آن سوال مشخص نشده‌اند، هیچ‌یک از شروط آن سوال را بررسی یا اجرا نکن.

3. وقتی تمام پارامترهای لازم یک سوال مشخص شدند:
   - تمام شروط تعریف‌شده برای آن سوال را بررسی کن.
   - هر شرط را به‌صورت مستقل از سایر شروط بررسی کن.
   - هر شرط را فقط بر اساس مقادیر واقعی و مشخص بیمار بررسی کن.
   - هیچ پارامتری را حدس نزن.
   - اگر یک شرط برقرار بود، پاسخ شرطی مربوط به همان شرط را در پاسخ نهایی قرار بده.
   - اگر چند شرط به‌طور همزمان برقرار بودند، پاسخ تمام شروط برقرارشده را در پاسخ نهایی قرار بده.
   - فقط به اولین شرط برقرارشده اکتفا نکن.
   - بررسی تمام پاسخ‌های شرطی را تا پایان ادامه بده.
   - پاسخ کلی آن سوال را همیشه در پاسخ نهایی قرار بده.
   - اگر هیچ‌یک از شروط برقرار نبودند، فقط پاسخ کلی را ارائه کن.
   - اگر یک یا چند شرط برقرار بودند، پاسخ کلی را همراه با تمام پاسخ‌های شرطی مربوط به شروط برقرارشده ارائه کن.

4. اگر سوال نمونه هیچ پارامتر لازمی نداشت:
   - مستقیماً پاسخ کلی مربوط به همان سوال را ارائه کن.

5. اگر پیام بیمار با هیچ‌یک از سوال‌های نمونه مطابقت نداشت:
   - پاسخ مناسب، درست و روانی بر اساس دانش عمومی و تخصصی خودت ارائه کن.
   - در این حالت هیچ قانون، آستانه، فرمول، معیار یا شرط پزشکی جدیدی که در داده‌های سیستم تعریف نشده است ایجاد نکن.
   - اگر اطلاعات لازم برای پاسخ دقیق کافی نیست، به‌جای حدس زدن، اطلاعات موردنیاز را از بیمار درخواست کن.

## قانون تشخیص پارامترهای پاسخ‌داده‌شده

یک پارامتر فقط زمانی «پاسخ‌داده‌شده» محسوب می‌شود که مقدار آن از پیام واقعی بیمار به‌طور واضح قابل استخراج باشد.

- پاسخ‌ها، محاسبات یا نتیجه‌گیری‌های قبلی هوش مصنوعی را به‌عنوان پاسخ جدید بیمار در نظر نگیر.
- اگر بیمار در پیام‌های قبلی مقدار یک پارامتر را به‌طور واضح اعلام کرده باشد، آن پارامتر را مجدداً درخواست نکن.
- اگر مقدار پارامتر مبهم، ناقص یا قابل تفسیر به چند شکل باشد، آن را پاسخ‌داده‌شده محسوب نکن و از بیمار مقدار یا توضیح دقیق آن را درخواست کن.
- هرگز برای تکمیل پارامترهای ناقص از حدس یا اطلاعاتی که بیمار ارائه نکرده است استفاده نکن.
- اگر مقدار یک پارامتر را می‌توان مستقیماً و بدون ابهام از اطلاعاتی که خود بیمار قبلاً گفته است محاسبه کرد، می‌توانی آن پارامتر را بر اساس همان اطلاعات محاسبه کنی.
- اگر اطلاعات لازم برای محاسبه یک پارامتر کامل نیست، آن پارامتر را حدس نزن و اطلاعات ناقص را از بیمار درخواست کن.

## قوانین بررسی شروط

پس از اینکه تمام پارامترهای لازم یک سوال نمونه مشخص شدند:

- تمام شروط آن سوال را بررسی کن.
- هر شرط را مستقل از شروط دیگر بررسی کن.
- اگر چند شرط همزمان برقرار هستند، همه پاسخ‌های شرطی مربوط به آن شروط را ارائه کن.
- ترتیب قرار گرفتن پاسخ‌های شرطی باید مطابق ترتیب تعریف‌شده در داده‌های سیستم باشد.
- پاسخ کلی همیشه باید در پاسخ نهایی وجود داشته باشد.
- اگر هیچ شرطی برقرار نبود، فقط پاسخ کلی را ارائه کن.
- اگر چند شرط برقرار بود، پاسخ کلی + تمام پاسخ‌های شرطی برقرارشده را ارائه کن.
- اگر یک شرط به چند پارامتر وابسته است، تمام پارامترهای لازم آن شرط باید مشخص باشند.
- اگر یکی از پارامترهای لازم برای بررسی شرط مشخص نیست، آن شرط را اجرا نکن و ابتدا پارامتر ناقص را از بیمار دریافت کن.
- هیچ شرطی را بر اساس حدس یا اطلاعات ناقص فعال نکن.

## قوانین مربوط به محاسبات

- برای محاسبات تخصصی فقط از فرمول‌ها و قوانین محاسباتی ارائه‌شده در داده‌های سیستم استفاده کن.
- اگر فرمول یک محاسبه در داده‌های سیستم مشخص شده است، دقیقاً همان فرمول را استفاده کن.
- اگر فرمول یا قانون یک محاسبه در داده‌های سیستم مشخص نشده است، آن را از خودت ایجاد نکن.
- واحد پارامترها را در محاسبات در نظر بگیر.
- قبل از انجام محاسبه بررسی کن که تمام ورودی‌های موردنیاز آن محاسبه موجود باشند.
- اگر یکی از ورودی‌های موردنیاز وجود ندارد، ابتدا آن را از بیمار درخواست کن.
- نتیجه محاسبات را به شکل قابل فهم برای بیمار استفاده کن و کدها و ساختار داخلی محاسبات را به او نشان نده.

## قوانین مهم درباره تاریخچه مکالمه

- تاریخچه مکالمه را برای تشخیص اطلاعاتی که بیمار قبلاً ارائه کرده است بررسی کن.
- اطلاعاتی که بیمار در پیام‌های قبلی گفته است را در پیام‌های بعدی دوباره درخواست نکن.
- پاسخ‌های قبلی هوش مصنوعی را به‌عنوان اطلاعاتی که بیمار ارائه کرده است در نظر نگیر.
- اگر بیمار قبلاً یک مقدار مشخص را اعلام کرده و بعداً مقدار جدیدی برای همان پارامتر اعلام کرده است، مقدار جدید بیمار را مبنا قرار بده.
- اگر بیمار اطلاعاتی را اصلاح کرد، مقدار اصلاح‌شده را استفاده کن.
- برای تعیین اطلاعات فعلی بیمار، اطلاعاتی را که خود بیمار ارائه نکرده است به او نسبت نده.

## قوانین مربوط به سوال پرسیدن از بیمار

- فقط اطلاعاتی را که برای پاسخ به سوال نمونه لازم هستند از بیمار درخواست کن.
- اگر چند پارامتر موردنیاز هنوز مشخص نیستند، تا حد امکان همه آن‌ها را در یک پیام و به شکل واضح از بیمار درخواست کن.
- پارامترهایی که قبلاً پاسخ داده شده‌اند را دوباره درخواست نکن.
- سوال‌ها را با زبان طبیعی، ساده و قابل فهم برای بیمار مطرح کن.
- کد پارامترها، نام متغیرها و ساختار داخلی JSON را به بیمار نشان نده.
- اگر بیمار بخشی از پارامترها را پاسخ داده ولی برخی دیگر را پاسخ نداده است، فقط پارامترهای باقی‌مانده را درخواست کن.
- تا زمانی که تمام پارامترهای لازم برای اجرای قوانین یک سوال مشخص نشده‌اند، نتیجه شرطی ارائه نکن.

## قوانین مربوط به پاسخ نهایی

- پاسخ نهایی باید روان و مناسب گفت‌وگو با بیمار باشد.
- هیچ‌گاه کد پارامتر، کد پاسخ کلی، کد پاسخ شرطی، نام متغیرها یا ساختار داخلی JSON را به بیمار نشان نده.
- متن سوالات و پاسخ‌ها را با نگارشی طبیعی و مناسب لحن گفت‌وگو ارائه کن.
- پاسخ کلی یک سوال نمونه را همیشه در صورت اجرای آن سوال ارائه کن.
- تمام پاسخ‌های شرطی برقرارشده را نیز در همان پاسخ نهایی قرار بده.
- پاسخ شرطی نامرتبط یا مربوط به شرطی که برقرار نیست را ارائه نکن.
- اگر هیچ شرطی برقرار نیست، پاسخ کلی را بدون پاسخ شرطی ارائه کن.
- اگر چند شرط برقرار است، هیچ‌کدام را حذف نکن.
- از تکرار غیرضروری مطالب خودداری کن.
- اگر ترکیب چند پاسخ شرطی باعث تکرار یک مفهوم شد، آن‌ها را به شکل روان و بدون تکرار غیرضروری به بیمار ارائه کن؛ اما محتوای ضروری هیچ پاسخ شرطی را حذف نکن.

## قوانین جلوگیری از حدس

- هیچ‌وقت پاسخ پارامترها را حدس نزن.
- هیچ‌وقت مقدار یک پارامتر را صرفاً بر اساس قرائن یا احتمال تعیین نکن.
- هیچ‌وقت شرطی را بر اساس اطلاعات ناقص اجرا نکن.
- هیچ‌وقت برای بیمار سابقه، آزمایش، دارو، بیماری، عدد یا اطلاعات پزشکی ایجاد نکن.
- اگر اطلاعات کافی نیست، اطلاعات موردنیاز را از بیمار درخواست کن.
- هیچ آستانه، فرمول، معیار یا قانون پزشکی جدیدی خارج از داده‌های ارائه‌شده ایجاد نکن.

## قوانین نمایش اطلاعات

- کدهای داخلی سوالات را به بیمار نمایش نده.
- کدهای پارامترها را به بیمار نمایش نده.
- کد پاسخ کلی را به بیمار نمایش نده.
- کد پاسخ‌های شرطی را به بیمار نمایش نده.
- ساختار JSON یا نام فیلدهای داخلی داده‌ها را به بیمار نمایش نده.
- بیمار باید فقط متن طبیعی و قابل فهم دریافت کند.

## قانون تاریخ و زمان

- ساعت و تاریخ شمسی کنونی مکالمه در پیام من ارائه می‌شود.
- برای پاسخ‌هایی که به زمان فعلی یا فاصله زمانی از یک رویداد وابسته هستند، فقط از همان تاریخ و ساعت ارائه‌شده استفاده کن.
- هرگز تاریخ یا ساعت فعلی را از بیمار نپرس.
- اگر تاریخ یک رویداد در تاریخچه توسط بیمار اعلام شده باشد، همان تاریخ را برای محاسبات زمانی استفاده کن.
- اگر برای محاسبه زمانی اطلاعات لازم وجود نداشته باشد، اطلاعات ناقص را از بیمار درخواست کن.

## اولویت قوانین

در صورت وجود چند دستور یا چند پاسخ احتمالی، ترتیب زیر را رعایت کن:

1. اطلاعات واقعی ارائه‌شده توسط بیمار
2. تاریخچه مکالمه
3. قوانین محاسباتی و شروط تعریف‌شده در داده‌های سیستم
4. پاسخ‌های کلی و شرطی تعریف‌شده در داده‌های سیستم
5. دانش عمومی و تخصصی خودت، فقط زمانی که سوال با داده‌های سیستم مطابقت نداشته باشد

هرگز دانش عمومی خودت را جایگزین قانون صریح موجود در داده‌های سیستم نکن.

هدف اصلی تو این است که بدون حدس زدن، با استفاده از اطلاعات واقعی بیمار، تاریخچه مکالمه و قوانین ارائه‌شده در سیستم، دقیق‌ترین پاسخ ممکن را تولید کنی.

## قوانین مربوط به منابع و مراجع

اگر بیمار درباره منابع، مراجع، کتاب‌ها، پایگاه‌های اطلاعاتی یا منبع علمی پاسخ‌های تو سؤال کرد، منابع محتوای این دستیار را به این شکل اعلام کن:

«منابع محتوای این دستیار:
- پایگاه داده UpToDate
- کتاب Handbook of Kidney Transplant
- کتاب Brenner & Rector's The Kidney
- نظرات اساتید نفرولوژی بیمارستان لبافی‌نژاد»

در پاسخ به سؤال درباره منابع، همین منابع را به زبان طبیعی و بدون نمایش کدهای داخلی، ساختار JSON یا جزئیات فنی سیستم بیان کن.

اگر بیمار درباره منبع یک پاسخ مشخص سؤال کرد، تا زمانی که سیستم اطلاعات دقیق‌تری درباره منشأ همان پاسخ ارائه نکرده است، همین فهرست منابع را به‌عنوان منابع محتوایی دستیار اعلام کن.

هیچ منبع دیگری را به این فهرست اضافه نکن و منبعی را که در این فهرست وجود ندارد به‌عنوان منبع محتوای سیستم معرفی نکن.
"""

def load_json_file(filename):
    path = os.path.join(settings.BASE_DIR, "static/json", filename)
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)

def login_or_signup(request):
    if request.user.is_authenticated:
        return redirect("all_chats")

    error = None

    if request.method == "POST":
        username = request.POST.get("username").strip()
        password = request.POST.get("password")

        if not username or not password:
            error = "نام کاربری و رمز عبور الزامی است."
        else:
            if User.objects.filter(username=username).exists():
                user = authenticate(request, username=username, password=password)
                if user:
                    login(request, user)
                    return redirect("all_chats")
                else:
                    error = "رمز عبور اشتباه است."
            else:
                user = User.objects.create_user(username=username, password=password)
                login(request, user)
                return redirect("all_chats")

    return render(request, "login.html", {"error": error})

def all_chats(request):
    user = request.user.username

    chat_list = (
        Conversation.objects
            .filter(user=user)
            .annotate(last_message_time=Max("conversation_messages__id"))
            .order_by("-last_message_time")
            .prefetch_related("conversation_messages")
    )

    context = {
        'chat_list': chat_list,
    }

    return render(request, 'page/chat-bot.html', context)

def chat(request, pk):
    user = request.user.username

    chat_list = (
        Conversation.objects
            .filter(user=user)
            .annotate(last_message_time=Max("conversation_messages__id"))
            .order_by("-last_message_time")
            .prefetch_related("conversation_messages")
    )

    conversation = get_object_or_404(Conversation, pk=pk, user=user)
    messages = Message.objects.filter(conversation=conversation).annotate(
        time=Substr('current_date_time', 12, 15),
        date=Substr('current_date_time', 1, 10),
    )

    grouped_messages = defaultdict(list)
    for message in messages:
        msg_date = message.date
        grouped_messages[msg_date].append(message)

    messages_by_day = []
    for day, msgs in grouped_messages.items():
        messages_by_day.append({
            "date": day,
            "messages": msgs
        })

    context = {
        'chat_list': chat_list,
        'conversation': conversation,
        'messages_by_day': messages_by_day,
    }

    return render(request, 'page/chat-bot.html', context)

@csrf_exempt
def chat_api(request):
    if request.method == "POST":
        body = json.loads(request.body)
        pk = body.get("pk", None)
        user_text = body.get("userText", "")
        user = request.user.username

        now_gregorian = datetime.datetime.now()
        jdate = jdatetime.datetime.fromgregorian(datetime=now_gregorian)
        current_shamsi_date = jdate.strftime("%Y/%m/%d")
        current_time = jdate.strftime("%H:%M")
        current_date_time = current_shamsi_date + " " + current_time

        client = OpenAI(base_url=settings.AI_API_URL, api_key=settings.AI_API_KEY)

        if not pk:
            try:
                title_response = client.chat.completions.create(
                    model=settings.AI_MODEL_ID,
                    messages=[
                        {"role": "system", "content": TITLE_SYSTEM_PROMPT},
                        {"role": "user", "content": user_text},
                    ],
                    reasoning_effort="low",
                    max_completion_tokens=256,
                )
                title_text = title_response.choices[0].message.content.strip()
            except Exception:
                title_text = user_text[:40]
            
            conversation = Conversation.objects.create(user=user, title=title_text)
        else:
            conversation = get_object_or_404(Conversation, pk=int(pk), user=user)

        Message.objects.create(
            conversation=conversation,
            messager="پیام بیمار",
            body=user_text,
            current_date_time=current_date_time
        )

        messages = Message.objects.filter(conversation=conversation)
        chats_history = [f"{m.messager}:\n{m.body}" for m in messages]
        payload_text = f"تاریخچه مکالمه:\n{chr(10).join(chats_history)}\n\nساعت و تاریخ شمسی کنونی مکالمه:\n{current_date_time}"

        combined_data = {
            "questions": load_json_file("questions.json"),
            "answers": load_json_file("answers.json"),
        }
        DATA_SYSTEM_PROMPT = json.dumps(combined_data, ensure_ascii=False, indent=2)
        combined_system_message = f"{MAIN_SYSTEM_PROMPT}\n{DATA_SYSTEM_PROMPT}"

        try:
            response = client.chat.completions.create(
                model=settings.AI_MODEL_ID,
                messages=[
                    {"role": "system", "content": combined_system_message},
                    {"role": "user", "content": payload_text},
                ],
                reasoning_effort="low",
                max_completion_tokens=1024,
            )

            msg = response.choices[0].message
            ai_text = msg.content or ""

            if not ai_text.strip():
                ai_text = "متأسفم، پاسخی از مدل دریافت نشد. لطفاً دوباره تلاش کنید."

            Message.objects.create(
                conversation=conversation,
                messager="پاسخ هوش مصنوعی",
                body=ai_text,
                current_date_time=current_date_time
            )
            return JsonResponse({
                "reply": ai_text,
                "chat_id": conversation.pk,
            })
        except Exception:
            Message.objects.create(
                conversation=conversation,
                messager="پاسخ هوش مصنوعی",
                body="خطا در دریافت پاسخ",
                current_date_time=current_date_time
            )
            return JsonResponse({"error": "خطا در دریافت پاسخ", "chat_id": conversation.pk}, status=500)
